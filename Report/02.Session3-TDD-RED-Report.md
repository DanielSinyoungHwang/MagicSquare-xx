# MagicSquare_xx — 세션 3 TDD RED 보고서

**작성일:** 2026-06-10  
**프로젝트:** MagicSquare_xx  
**세션:** 3 — `validate_lines` Harness · 규칙 · TDD RED  
**상태:** RED 1건 완료 → GREEN 대기 · **Export 완료**

---

## 1. 세션 목표

Mom Test STEP 1 근거로 문제 정의·구현 기반을 마련하고, 세션 3 Skill `validate_lines(grid)`에 대해 **TDD RED** 1건을 고정한다.

---

## 2. 산출물

| 파일 | 설명 |
|------|------|
| `Report/01.MagicSquare_ProblemDefinition_Report.md` | 문제 정의·R-G-I-O·S1~S3 (갱신) |
| `docs/PRD.md` | v0.1 PRD (갱신) |
| `README.md` | 프로젝트 소개·사용 예 |
| `magicxx/` | Rule·Command·Skill·Test Loop (`verify_grid`) |
| `.cursorrules` | 세션 3 도메인·API·ECB·TDD·AI 규칙 |
| `.cursor/commands/tdd-red.md` | RED 전용 Cursor Command |
| `.cursor/commands/export-session.md` | 세션 Export Cursor Command |
| `src/validate_lines.py` | 시그니처만 (`...`) |
| `tests/test_validate_lines.py` | S1 RED 테스트 1건 |
| `c:\DEV\MagicSquare_1004\` | 별도 Harness 골격 (참고용) |

---

## 3. 핵심 결정

| 항목 | 결정 |
|------|------|
| **API** | `validate_lines(grid) -> {status, failed_lines}` |
| **10선 명명** | R1~R4, C1~C4, D1, D2 |
| **status** | `pass` \| `fail` \| `incomplete` |
| **RED 범위** | `tests/`만 수정, `src/` 구현 금지 |
| **기존 `magicxx/`** | 유지. `src/validate_lines`로 세션 3 Skill 병행 |

---

## 4. 검증 결과

### 4.1 `validate_lines` vs 문제 정의 (리뷰)

- 10조건·합 34·S3 주대각 FAIL 방향 **일치**
- **어긋남**: `validate_lines` 미분리, INCOMPLETE 3값, 구조 오류 출력, S3 부대각 단독 회귀 부재 등 (`.cursorrules` 보완 후보)

### 4.2 pytest (Export 시점)

```bash
python -m pytest tests/test_validate_lines.py tests/test_verify.py -v
```

| 구분 | 통과 | 실패 | 합계 |
|------|------|------|------|
| `test_validate_lines.py` | 0 | 1 | 1 |
| `test_verify.py` | 10 | 0 | 10 |
| **합계** | **10** | **1** | **11** |

| 테스트 | 결과 | 메시지 |
|--------|------|--------|
| `test_complete_grid_all_ten_lines_pass` | **FAILED** | `TypeError: 'NoneType' object is not subscriptable` |

**겨냥 기준:** **S1** — 완성 4×4 격자 10선 일괄 pass

---

## 5. 다음 액션

- [ ] **GREEN**: `src/validate_lines.py` 최소 구현 → 위 테스트 PASS
- [ ] **RED** 추가: S2 (`failed_lines` 상세), S3 (D1 FAIL 회귀)
- [ ] `src/rules.py`에 `MAGIC_SUM` SSOT 도입
- [ ] `.cursorrules` 빠진 규칙 반영 (선택)

---

## 6. 관련 문서

- `Prompting/02.Session3-TDD-RED-Transcript.md`
- `Report/01.MagicSquare_ProblemDefinition_Report.md`
- `Report/01.Mom-Test-STEP1-Report.md`
- `.cursor/commands/tdd-red.md`
- `.cursorrules`
